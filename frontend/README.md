# Frontend (React + Vite)

Setup and scripts for this app live in the **[repository root README](../README.md)**.

Quick start:

```bash
npm install
npm run dev
```

Ensure the FastAPI backend is running (default API URL `http://localhost:8000`) unless `VITE_API_URL` is set.
